@model IEnumerable<Library.ViewModels.Models.PublicationHouseViewModel>

@{
    ViewBag.Title = "PublicationHouse List";
}

<h3>Publication Houses</h3>

<div>
    @if (User.IsInRole("Admin"))
    {
    <div>
        @using (Html.BeginForm("AddPublicationHouse", "PublicationHouse", FormMethod.Get))
    {
        <button class="btn btn-lg btn-primary" type="submit">Add Publication House</button>
}
    </div>
    }
    <br>
    <div id="HouseGrid"></div>
</div>
<br>
<div>
    @Html.ActionLink("To Main Page", "Index", "Publication")
</div>